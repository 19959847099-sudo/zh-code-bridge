# ZhCode Bridge / 中文代码桥
# AI 协作开发规则 V0.1

> 基于《AI 协作开发规则总纲 V1》与 V1.1 增补条款调整而成。  
> 本文件是 ZhCode Bridge 项目的 AI 协作施工控制规则，不是项目定义稿，也不是开发流程设计。

---

## 0. 文档定位

本文件用于规范 ZhCode Bridge 项目中「你 + AI / Codex」的协作开发方式。

它回答的问题是：

> 在 ZhCode Bridge 的开发过程中，AI 应该如何施工、如何受控、如何回执、如何处理失败、如何保持项目状态可交接。

三份文档分工如下：

```text
项目定义稿：
定义 ZhCode Bridge 是什么，边界是什么，V0.1 要实现什么。

开发流程设计：
定义从 0 到 V0.1 最终版分哪些阶段、哪些轮次、每轮做什么。

AI 协作开发规则：
定义你和 AI / Codex 如何协作，AI 有什么权限，每轮如何验收，失败如何处理，日志如何沉淀。
```

本文件不替代项目定义稿。  
本文件不替代开发流程设计。  
本文件只负责控制 AI 施工过程。

---

## 1. 第一原则

### 1.1 先边界，后施工

未确认本轮 Scope / Action / Done，不进入正式施工。

AI 不得在边界不清楚时自行开工。

### 1.2 先闭环，后扩展

每一轮优先完成当前轮的主任务。

新想法、新功能、新方向默认记录，不并入当前轮。

### 1.3 快速推进，但必须可控

ZhCode Bridge 采用“高标准快速原型 + 后续真实调试”的策略。

允许快速推进，但必须满足：

```text
1. 本轮目标明确
2. 修改范围可控
3. 主链不被破坏
4. 结果可验证
5. 回执可复盘
```

### 1.4 AI 是受控施工者，不是自由产品经理

AI 可以提出建议，但不得自行改变产品方向。

AI 不能自行决定：

```text
1. 改变 .zhjs / .js 文件模型
2. 改变符号不变、语法不变原则
3. 删除基础入口词表
4. 将 .zhjs 设计成直接运行语言
5. 改变 V0.1 主入口为非 VS Code 插件
6. 擅自新增 Python / Web Playground / AI 功能 / 独立 IDE
```

### 1.5 主链稳定优先

所有施工优先保证 ZhCode Bridge 主链稳定。

主链定义见第 4 节。

---

## 2. 三文档体系

ZhCode Bridge 采用三文档体系。

### 2.1 项目定义稿

作用：

```text
定义项目是什么
定义核心概念
定义硬规则
定义 V0.1 范围
定义入口词表
定义验收标准
```

状态：

```text
开工前形成
作为上位定义
不得在施工中随意修改
```

如果开发中出现新理解，不直接修改项目定义稿，而是先进入开发日志或后续版本讨论。

### 2.2 开发流程设计

作用：

```text
定义从 0 到 V0.1 的阶段
定义 P / B / R 三阶段
定义每个阶段下的施工轮次
定义每轮目标、范围和完成标准
定义 V0.1 最终产物
```

状态：

```text
指导施工路线
可在阶段收口时做版本化调整
不得在单轮施工中随意改动
```

### 2.3 AI 协作开发规则

作用：

```text
定义 AI 如何施工
定义 AI 权限边界
定义小轮回执
定义失败轮
定义开发日志
定义聊天切换对齐
```

状态：

```text
稳定生效
用于控制 AI 施工过程
```

---

## 3. 阶段与轮次体系

ZhCode Bridge 从 0 到 V0.1 最终版分为三个大阶段：

```text
P 阶段：Prototype / 原型阶段
B 阶段：Beta / 打磨阶段
R 阶段：Release / 收口阶段
```

---

## 4. ZhCode Bridge 主链定义

本项目主链是：

```text
.zhjs 文件识别
        ↓
中文入口词转译
        ↓
生成同名 .js 文件
        ↓
运行生成后的 .js 文件
        ↓
Output 面板显示结果
        ↓
可查看生成后的标准 JavaScript
```

任何导致以上链路中断的改动，都视为高风险改动。

以下情况视为主链受损：

```text
1. .zhjs 无法被 VS Code 识别
2. transformZhjsToJs 无法正常工作
3. 字符串 / 注释中的入口词被严重误替换
4. 无法生成同名 .js
5. 生成的 .js 大面积语法错误
6. Run Current File 不可用
7. Output 面板无法显示结果
8. Show Generated JavaScript 不可用
9. 插件无法启动
10. npm run compile 失败
```

主链受损时，不得继续堆新功能，必须优先修复。

---

## 5. 核心硬规则

以下规则为 ZhCode Bridge 的宪法级规则。

AI 不得自行修改、绕过或弱化。

### 5.1 符号不变

ZhCode Bridge 不改变 JavaScript 原有符号系统。

保留：

```text
()
{}
[]
;
,
.
:
" "
' '
` `
+ - * / %
= == === != !==
> < >= <=
&& || !
```

### 5.2 语法结构不变

允许：

```js
如果 (分数 >= 60) {
  打印("通过");
} 否则 {
  打印("不通过");
}
```

不允许设计成：

```text
如果 分数 大于等于 60：
    打印 “通过”
否则：
    打印 “不通过”
```

AI 不得设计自然语言式中文语法。

### 5.3 `.zhjs` 不直接运行

`.zhjs` 是中文表层文本文件。

真正运行的是生成后的 `.js` 文件。

固定链路：

```text
.zhjs → 生成 .js → 运行 .js
```

### 5.4 基础入口词不做阉割

V0.1 不允许只支持几个演示词。

必须覆盖 JavaScript 入门阶段基础入口词：

```text
keyword
literal
operatorWord
apiFunction
method
```

包括语言结构、基础值、操作判断、输出、类型转换、字符串方法、数组方法、对象入口、JSON、Math、Date 等。

AI 不得为了降低实现难度擅自删减词表。

### 5.5 VS Code 插件是 V0.1 主入口

V0.1 主产品形态是 VS Code 插件。

AI 不得擅自改成：

```text
Web Playground
独立 IDE
中文命令行
浏览器应用
课程系统
```

---

## 6. 阶段编码规则

轮次编号格式：

```text
阶段字母 + 轮次编号 + 类型字母
```

示例：

```text
P1-S
P2-S
P3-S
P4-I
P5-U
P6-O
B1-F
B2-U
B3-T
B4-O
B5-F
R1-O
R2-O
```

类型字母含义：

```text
S = Structure / 结构
I = Integration / 能力接入
U = User Experience / 用户体验
F = Fix / 修复
T = Table & Tuning / 词表校准与调试
O = Optimization & Close / 优化与收口
```

一次正式施工算一轮。

---

## 7. 小轮任务定义规则

每一轮施工前，必须明确：

```text
Scope
Action
Done
```

### 7.1 Scope：修改范围

Scope 用于说明本轮允许触达哪些模块、文件或能力。

示例：

```text
本轮 Scope：
src/compiler/entries.ts
src/compiler/types.ts
src/compiler/index.ts
```

AI 不得超出 Scope 自行修改无关模块。

### 7.2 Action：执行动作

Action 用于说明本轮要做什么。

示例：

```text
本轮 Action：
1. 定义 EntryKind
2. 定义 ZhCodeEntry
3. 建立完整 V0.1 基础入口词表
4. 提供按 kind 获取词条的工具函数
```

### 7.3 Done：完成标准

Done 必须清晰、可判断、可验证。

示例：

```text
本轮 Done：
1. 五类入口词都有覆盖
2. 每个词条至少包含 zh、target、kind、category、description
3. npm run compile 成功
4. 词表没有散落到多个文件
```

禁止使用无法判断的 Done：

```text
优化一下
做得更自然
尽量完善
稍微整理
```

---

## 8. 施工前读取要求

每一轮正式施工前，AI 必须确认已读取或接收以下信息：

```text
1. 当前项目定义稿中的相关边界
2. 当前开发流程设计中的本轮 Scope / Action / Done
3. 最近一轮施工回执或阶段开发日志
4. 当前仓库真实状态
```

未完成状态读取前，不得直接施工。

如果缺少关键信息，AI 必须先请求补充或进行状态对齐。

---

## 9. AI 施工权限

### 9.1 允许直接施工

满足以下条件时，AI 可以直接施工：

```text
1. 当前轮 Scope 已经明确
2. 当前轮 Action 已经明确
3. 当前轮 Done 已经明确
4. 修改完全处于本轮范围内
5. 不改变项目硬规则
6. 不改变主链方向
```

### 9.2 需要先确认再施工

出现以下情况，AI 必须先说明方案，等待确认：

```text
1. 需要超出当前 Scope
2. 涉及主链结构调整
3. 涉及多模块联动且当前轮未明确允许
4. 涉及 package.json 构建链调整
5. 涉及生成文件覆盖策略变化
6. 涉及词表大规模重命名
7. 涉及入口词分类规则变化
8. 涉及 VS Code 插件 activationEvents / contributes 大幅调整
```

### 9.3 禁止自行执行

以下事情 AI 不得自行执行：

```text
1. 修改项目定义稿
2. 改变 .zhjs / .js 文件模型
3. 改变符号不变、语法不变规则
4. 删除基础入口词表
5. 把 .zhjs 设计成直接运行语言
6. 新增 Python / TypeScript / DOM / Node API 中文化
7. 新增 Web Playground / 独立 IDE / AI 功能
8. 进行大规模重构
9. 擅自进入下一阶段
10. 擅自发布 Marketplace
```

---

## 10. 快速直推规则

ZhCode Bridge 采用高标准快速原型策略。

因此，在本轮 Scope 明确的情况下，允许 AI 快速直推。

### 10.1 允许快速直推的情况

```text
1. 本轮 Scope 已明确允许对应文件修改
2. 修改服务于当前 Done
3. 不改变项目硬规则
4. 不破坏主链
5. 不新增当前轮之外的功能
```

例如：

```text
P4-I 本来就允许修改 commands.ts、generate.ts、output.ts。
因此在 P4-I 内，这些文件的联动修改属于本轮范围，不需要额外确认。
```

### 10.2 不允许快速直推的情况

```text
1. 超出本轮 Scope
2. 修改项目架构方向
3. 改变文件模型
4. 删除或缩减基础词表
5. 把 bug 修复扩展成新功能
6. 把体验优化扩展成产品方向变更
```

### 10.3 无法判断时的默认行为

如果 AI 无法判断是否越界，默认停止施工并请求确认。

不得以“改动不大”为理由自行推进。

---

## 11. 优先级裁决规则

当同一轮中同时出现多个问题，默认优先级为：

```text
主链稳定
    >
当前轮 Done 达成
    >
P0 / P1 风险修复
    >
体验优化
    >
文档优化
    >
新想法记录
```

### 11.1 主链优先

如果主链不可用，优先修主链。

不得在主链不可用时继续做 Hover 文案、README 美化、示例增补等非核心事项。

### 11.2 Done 优先

当前轮 Done 没完成时，不得擅自切换到下一轮任务。

### 11.3 新想法默认记录

新想法默认记录，不并入当前轮。

只有当新想法直接服务于当前 Done，且不会引入新风险时，才允许纳入当前轮。

---

## 12. 问题分级

问题分为 P0 / P1 / P2 / P3。

### 12.1 P0：阻塞问题

定义：

```text
主链不可用、构建失败、插件无法启动、无法运行 .zhjs、生成 JS 严重错误。
```

示例：

```text
npm run compile 失败
VS Code 插件无法启动
.zhjs 无法识别
Run Current File 完全不可用
生成的 .js 大面积语法错误
```

处理原则：

```text
P0 必须立即修复，不能进入下一阶段。
```

### 12.2 P1：严重问题

定义：

```text
核心体验受损，但项目不是完全不可用。
```

示例：

```text
核心入口词转译错误
字符串 / 注释误替换
非生成 .js 被静默覆盖
examples 大面积失败
Show Generated JavaScript 不可用
```

处理原则：

```text
P1 必须在进入 R 阶段前修复。
```

### 12.3 P2：普通问题

定义：

```text
影响使用体验，但不阻断主链。
```

示例：

```text
Hover 文案不够好
补全排序不理想
Output 格式不够清晰
README 表达可优化
某个边缘词解释不够准确
```

处理原则：

```text
P2 优先在 B 阶段修复；如不影响 V0.1 质量，可记录到 V0.1.x。
```

### 12.4 P3：低优先级问题

定义：

```text
不影响 V0.1 交付的小问题或未来想法。
```

示例：

```text
视觉细节
非核心 examples 增补
文案微调
未来功能建议
```

处理原则：

```text
P3 默认记录，不阻塞 V0.1。
```

---

## 13. 小轮施工规则

### 13.1 每轮必须围绕一个明确目标

每轮只能围绕当前轮目标推进。

例如：

```text
P3-S：转译核心轮
```

就不应该同时大幅改 README、做 Marketplace 发布、添加 Web Playground。

### 13.2 每轮可以多文件联动，但必须在 Scope 内

ZhCode Bridge 某些轮次天然需要多文件联动。

例如：

```text
P4-I：文件生成与运行主链轮
```

可能涉及：

```text
generate.ts
commands.ts
output.ts
utils/file.ts
utils/node.ts
extension.ts
```

只要这些文件在 Scope 内，就允许联动。

### 13.3 不允许“顺手加功能”

AI 不能因为“顺手”就做当前轮之外的东西。

例如：

```text
在 P3-S 转译核心轮中，顺手做 Web Playground —— 禁止。
在 P5-U 插件体验轮中，顺手新增 Python 支持 —— 禁止。
```

---

## 14. 小轮施工回执

每一轮施工完成后，AI 必须输出施工回执。

回执必须简洁，但足够支撑验收和续接。

### 14.1 标准回执模板

```text
当前轮次：
本轮目标：

完成内容：
- 
- 

修改文件：
- 
- 

Done 对照：
1. 
2. 
3. 

测试 / 验证：
- 

是否触及主链：
是 / 否
说明：

是否越界：
是 / 否
说明：

遗留问题：
- 

下一轮建议：
- 
```

### 14.2 回执要求

```text
1. 先给结论，再给细节
2. 不写冗长过程
3. 必须对照 Done 判断是否完成
4. 必须说明是否触及主链
5. 必须说明是否越界
6. 必须列出遗留问题
```

---

## 15. 失败轮规则

### 15.1 失败轮定义

如果本轮施工后未达到 Done，且不能在同轮快速补救完成，则判定为失败轮。

AI 不得把失败轮模糊描述为：

```text
基本完成
主体完成
差不多完成
后续优化即可
```

除非你明确批准带病推进。

### 15.2 失败轮必须输出失败回执

失败回执模板：

```text
当前轮次：
本轮目标：

失败原因：
当前停留点：
已完成部分：
未完成部分：
是否影响主链：
是否建议回退：

建议后续路径：
修复轮 / 重定义轮 / 回退轮

是否建议立即继续推进：
是 / 否
```

### 15.3 失败后的三种路径

失败轮之后只能进入以下路径之一：

```text
1. 修复轮
当前方向正确，但需要追加修复。

2. 重定义轮
当前 Scope / Done 设定不合理，需要重新定义任务。

3. 回退轮
本轮改动影响主链或稳定性，需要回退。
```

### 15.4 主链损坏时必须修复

如果出现以下情况，必须优先修复：

```text
npm run compile 失败
插件无法启动
.zhjs 无法识别
转译核心不可用
Run Current File 不可用
生成 .js 大面积错误
```

不得直接进入下一正常轮。

---

## 16. 开发日志规则

开发日志默认在每个大阶段结束后产出。

即：

```text
P 阶段结束后：输出 P 阶段开发日志
B 阶段结束后：输出 B 阶段开发日志
R 阶段结束后：输出 V0.1 完整开发日志
```

### 16.1 开发日志作用

开发日志用于记录阶段真实状态，不是复述全部过程。

它回答：

```text
本阶段目标是什么
实际完成了什么
当前项目真实状态如何
还有哪些问题
下一阶段怎么接
```

### 16.2 阶段开发日志模板

```text
阶段名称：
阶段目标：

实际完成内容：
- 
- 

当前真实状态：
- 

主链状态：
稳定 / 不稳定
说明：

已知问题：
- 

临时偏差：
- 

下一阶段入口：
- 

交接说明：
- 
```

---

## 17. 聊天切换与状态对齐

聊天切换视为协作环境切换。

切换后不得直接继续开发，必须先对齐状态。

### 17.1 状态对齐内容

至少包含：

```text
当前项目状态
当前所处阶段 / 轮次
最近一轮完成内容
主链是否稳定
已知问题
下一轮目标
```

### 17.2 未完成阶段中的聊天切换

如果大阶段尚未结束，但发生聊天切换，应输出临时状态对齐说明。

模板：

```text
当前阶段 / 轮次：
最近完成：
当前主链状态：
当前未完成：
下一步建议：
需要注意：
```

---

## 18. R 阶段功能冻结规则

进入 R 阶段后，项目进入功能冻结状态。

### 18.1 冻结后允许

```text
修 bug
修文档
修 examples
修测试
修打包问题
修阻塞体验问题
```

### 18.2 冻结后禁止

```text
新增入口词大类
新增 Python / TypeScript / DOM / Node API
新增 AI 功能
新增 Web Playground
改变文件模型
改变核心语法规则
重构主链架构
```

---

## 19. V0.1 最终验收

V0.1 最终验收以开发流程设计中的最终验收标准为准。

但 AI 协作层面必须额外确认：

```text
1. 所有阶段日志已产出
2. 最后一轮施工回执已产出
3. P0 / P1 问题已清除
4. 已知 P2 / P3 问题已记录
5. V0.1 产物清单已确认
6. .vsix 已打包
7. README 已完成
8. examples 可运行
9. 未违反项目定义稿硬规则
```

---

## 20. 输出最小化原则

所有回执、日志、对齐说明，都遵循：

> 足够支撑验收、续接、复盘即可。

不以篇幅长短作为质量标准。

要求：

```text
1. 优先给结论
2. 再给必要细节
3. 不堆叠冗长过程
4. 不展开无关函数级细节
5. 不为了显得完整而写长
```

如果一份日志无法让新聊天在 3 分钟内理解当前状态，说明信息组织有问题。

---

## 21. 最终原则

ZhCode Bridge 的 AI 协作开发目标是：

> **高效推进，但必须可控；快速施工，但不能失去边界；项目可以快速成型，但不能做成失控的半成品。**

AI 的角色是：

```text
受控施工者
实现协作者
测试辅助者
状态记录者
```

AI 不是：

```text
自由产品经理
项目方向决策者
规则修改者
边界突破者
```

最终裁决权归你。
