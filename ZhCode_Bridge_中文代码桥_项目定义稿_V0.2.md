# ZhCode Bridge / 中文代码桥 · 项目定义稿 V0.2

> **用中文入口词学习真实 JavaScript**

## 核心判断

ZhCode Bridge 不是一门新的中文编程语言，而是一座桥：

- `.zhjs` 是中文表层文本文件
- `.js` 是生成后的标准 JavaScript 文件
- 真正运行的是 `.js`

**版本：** V0.2 重排版  
**协议：** MIT  
**主入口：** VS Code 插件

---

## 目录

1. [项目一句话](#1-项目一句话)
2. [项目核心概念](#2-项目核心概念)
3. [核心硬规则](#3-核心硬规则)
4. [文件模型](#4-文件模型)
5. [项目定位](#5-项目定位)
6. [V0.1 产品目标](#6-v01-产品目标)
7. [VS Code 插件体验](#7-vs-code-插件体验)
8. [JavaScript 中文入口词表 V0.1](#8-javascript-中文入口词表-v01)
9. [入口词详细定义](#9-入口词详细定义)
10. [转译规则](#10-转译规则)
11. [Hover / 补全 / 文档要求](#11-hover--补全--文档要求)
12. [基础中文错误解释](#12-基础中文错误解释)
13. [README 第一屏要求](#13-readme-第一屏要求)
14. [V0.1 验收标准](#14-v01-验收标准)
15. [最终一句话](#15-最终一句话)

---

## 1. 项目一句话

ZhCode Bridge 是一个面向中文母语编程初学者的开源 VS Code 插件，让用户用中文入口词书写接近真实 JavaScript 语法结构的 `.zhjs` 表层文件，并自动生成标准 `.js` 文件运行，从而先理解编程结构，再逐步迁移到真实 JavaScript 生态。

---

## 2. 项目核心概念

ZhCode Bridge 的核心不是“中文替代 JavaScript”，而是：

> **中文表层协议 + 标准 JavaScript 执行层 + VS Code 使用体验**

用户主要编辑 `.zhjs`，系统根据 `.zhjs` 生成同名 `.js`，实际运行的是生成后的标准 JavaScript 文件。

### 核心链路

```text
.zhjs 中文表层文本文件
        ↓
ZhCode Bridge 转译
        ↓
标准 .js 文件
        ↓
Node / VS Code / JavaScript 生态真实运行
```

---

## 3. 核心硬规则

### 3.1 符号不变

ZhCode Bridge 不改变 JavaScript 原有符号系统。用户仍然使用 JavaScript 原本的括号、点号、引号、分号、逗号和运算符。

### 保留的 JavaScript 符号系统

```text
()  {}  []  ;  ,  .  :
" "  ' '  ` `
+ - * / %
= == === != !==
> < >= <=
&& || !
```

---

### 3.2 语法结构不变

项目只替换入口词，不重新设计语法。

### 允许：中文入口词 + 原 JavaScript 符号结构

```js
如果 (分数 >= 60) {
  打印("通过");
} 否则 {
  打印("不通过");
}
```

### 不允许：自然语言式中文语法

```text
如果 分数 大于等于 60：
    打印 “通过”
否则：
    打印 “不通过”
```

---

### 3.3 只做中文入口词映射

项目只把 `.zhjs` 表层文件里的中文入口词转成标准 JavaScript 词或 API。

| 中文入口词 | 生成 JS | 类型 |
|---|---|---|
| 如果 | if | 语言结构词 |
| 声明变量 | let | 语言结构词 |
| 函数 | function | 语言结构词 |
| 返回 | return | 语言结构词 |
| 打印 | console.log | API 函数入口 |
| 转大写 | toUpperCase | 点号方法入口 |
| 添加 | push | 点号方法入口 |
| 解析JSON | JSON.parse | API 函数入口 |

---

### 3.4 `.zhjs` 不直接参与运行

`.zhjs` 是中文表层文本文件，不直接运行。真正运行的是生成后的 `.js` 文件。

---

## 4. 文件模型

### 4.1 表层文件：`.zhjs`

`.zhjs` 是用户主要编辑的中文入口词文件。

#### score.zhjs

```js
函数 判断成绩(分数) {
  如果 (分数 >= 60) {
    返回 "通过";
  } 否则 {
    返回 "不通过";
  }
}

打印(判断成绩(80));
```

---

### 4.2 执行文件：`.js`

`.js` 是由 `.zhjs` 生成的标准 JavaScript 文件，是真正参与运行的文件。

#### score.js

```js
function 判断成绩(分数) {
  if (分数 >= 60) {
    return "通过";
  } else {
    return "不通过";
  }
}

console.log(判断成绩(80));
```

---

### 4.3 同名生成规则

| 用户创建 | 系统生成 |
|---|---|
| hello.zhjs | hello.js |
| score.zhjs | score.js |
| 判断成绩.zhjs | 判断成绩.js |

---

### 4.4 生成文件头部注释

生成文件建议头部：

```js
// Generated by ZhCode Bridge from score.zhjs.
// This file is standard JavaScript.
// Edit score.zhjs if you want to continue using Chinese entry words.

// 此文件由 ZhCode Bridge 根据 score.zhjs 自动生成。
// 这是标准 JavaScript 文件。
// 如果你想继续使用中文入口词，请编辑 score.zhjs。
```

---

## 5. 项目定位

### 5.1 它是什么

ZhCode Bridge 是：

- 中文入口词编程桥
- VS Code 插件
- JavaScript 基础学习辅助工具
- `.zhjs → .js` 转译工具
- 中文母语者进入真实 JavaScript 生态的过渡层

---

### 5.2 它不是什么

ZhCode Bridge 不是：

- 中文编程语言
- 易语言替代品
- 自然语言编程工具
- 自研 JavaScript 运行时
- 自研 IDE
- 中文命令行
- 中文 npm 生态
- 完整 JavaScript 生态封装

---

## 6. V0.1 产品目标

> **V0.1 的目标不是做一个“几个词能跑”的演示版，而是完整覆盖 JavaScript 入门阶段的基础入口词，让用户拥有尽量完整的基础编程体验。**

V0.1 不做完整 JavaScript 生态，不做 DOM、Node 文件系统、npm 包中文化、前端框架、TypeScript。

但 V0.1 要完整覆盖 JavaScript 基础结构、基础值、基础输出、类型转换、字符串、数组、对象、JSON、Math、Date 等入门阶段核心能力。

---

## 7. VS Code 插件体验

### 7.1 必须支持

- 识别 `.zhjs` 文件
- 中文入口词语法高亮
- 中文入口词自动补全
- Hover 中文解释
- 一键生成同名 `.js`
- 一键运行生成后的 `.js`
- Output 面板显示运行结果
- 查看生成后的标准 JavaScript
- 基础中文错误解释
- README 清楚展示项目概念

---

### 7.2 插件命令

| 命令 | 中文显示 | 作用 |
|---|---|---|
| ZhCode: Run Current File | 中文代码桥：运行当前文件 | 生成并运行当前 `.zhjs` 对应的 `.js` |
| ZhCode: Generate JavaScript | 中文代码桥：生成 JavaScript | 生成同名 `.js` 文件 |
| ZhCode: Show Generated JavaScript | 中文代码桥：查看生成的 JavaScript | 打开生成代码预览 |

---

## 8. JavaScript 中文入口词表 V0.1

V0.1 不删基础词。凡是 JavaScript 入门基础体验中高频出现的结构词、基础值、基础 API、基础方法，都应该进入词表。工程上通过分类规则实现，而不是靠砍词降低复杂度。

### 8.1 入口词分类

| 类别 | 说明 | 示例 |
|---|---|---|
| keyword | 语言结构词 | 如果 → if；函数 → function |
| literal | 基础值 | 真 → true；空 → null |
| operatorWord | 操作/判断词 | 类型 → typeof；属于 → in |
| apiFunction | API 函数入口 | 打印(...) → console.log(...) |
| method | 点号方法入口 | .添加(...) → .push(...) |

---

## 9. 入口词详细定义

## 9.1 keyword：语言结构词

### 变量声明

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 声明变量 | let | 声明一个可以重新赋值的变量 |
| 声明常量 | const | 声明一个不应该重新赋值的名字 |
| 旧式变量 | var | 旧式变量声明方式，不建议优先使用 |

### 条件判断

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 如果 | if | 条件成立时执行代码块 |
| 否则如果 | else if | 前面条件不成立时继续判断 |
| 否则 | else | 前面条件都不成立时执行 |

### 多分支判断

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 分支 | switch | 根据一个值进入不同分支 |
| 情况 | case | 某一种匹配情况 |
| 默认 | default | 没有匹配情况时执行 |
| 跳出 | break | 跳出 switch 或循环 |

### 循环

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 循环 | for | 按指定规则重复执行 |
| 当 | while | 条件成立时持续执行 |
| 执行 | do | 先执行一次，再判断条件 |
| 跳出 | break | 结束当前循环 |
| 继续 | continue | 跳过本轮，进入下一轮 |

### 函数

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 函数 | function | 定义一段可重复调用的逻辑 |
| 返回 | return | 把结果交回调用函数的位置，并结束当前函数 |

### 类和对象结构

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 类 | class | 定义一类对象 |
| 新建 | new | 创建实例 |
| 当前对象 | this | 指向当前对象本身 |
| 继承 | extends | 一个类继承另一个类 |
| 父类 | super | 调用父类构造或方法 |
| 构造函数 | constructor | 类实例创建时执行的特殊方法 |

### 错误处理

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 尝试 | try | 尝试执行可能出错的代码 |
| 捕获 | catch | 捕获错误 |
| 最终 | finally | 无论是否出错都会执行 |
| 抛出 | throw | 主动抛出错误 |

### 异步

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 异步 | async | 声明异步函数 |
| 等待 | await | 等待异步结果完成 |

### 模块

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 导入 | import | 导入模块 |
| 导出 | export | 导出内容 |
| 从 | from | 表示导入来源 |

---

## 9.2 literal：基础值

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 真 | true | 布尔真 |
| 假 | false | 布尔假 |
| 空 | null | 有意为空 |
| 未定义 | undefined | 尚未定义或不存在 |
| 不是数字 | NaN | 非有效数字 |
| 无穷大 | Infinity | 无穷大 |

---

## 9.3 operatorWord：操作/判断词

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 类型 | typeof | 查看值的类型 |
| 属于 | in | 判断属性是否在对象中 |
| 实例于 | instanceof | 判断对象是否是某个类的实例 |
| 删除属性 | delete | 删除对象属性 |

---

## 9.4 apiFunction：API 函数入口

这类入口词只在函数调用形式中替换，例如 `打印(...) → console.log(...)`，不应在普通变量名中随意替换。

### 输出

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 打印 | console.log | 输出内容 |
| 警告 | console.warn | 输出警告 |
| 输出错误 | console.error | 输出错误 |

### 类型转换

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 转整数 | parseInt | 转成整数 |
| 转小数 | parseFloat | 转成小数 |
| 转数字 | Number | 转成数字 |
| 转字符串 | String | 转成字符串 |
| 转布尔 | Boolean | 转成布尔值 |
| 是否不是数字 | isNaN | 判断是否不是有效数字 |
| 是否有限 | isFinite | 判断是否是有限数字 |

### 对象方法入口

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 对象键 | Object.keys | 获取对象所有键 |
| 对象值 | Object.values | 获取对象所有值 |
| 对象条目 | Object.entries | 获取对象键值对 |
| 合并对象 | Object.assign | 合并对象 |

### JSON

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 解析JSON | JSON.parse | 把 JSON 字符串转成对象 |
| 转为JSON | JSON.stringify | 把对象转成 JSON 字符串 |

### Math

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 随机数 | Math.random | 生成 0 到 1 之间的随机数 |
| 向下取整 | Math.floor | 向下取整 |
| 向上取整 | Math.ceil | 向上取整 |
| 四舍五入 | Math.round | 四舍五入 |
| 最大值 | Math.max | 获取最大值 |
| 最小值 | Math.min | 获取最小值 |
| 绝对值 | Math.abs | 获取绝对值 |
| 幂 | Math.pow | 计算次方 |
| 平方根 | Math.sqrt | 计算平方根 |

### Date

| 中文入口词 | 生成 JS | 说明 |
|---|---|---|
| 日期 | Date | 日期对象 |
| 当前时间 | Date.now | 当前时间戳 |

---

## 9.5 method：点号方法入口

这类入口词只在点号方法调用形式中替换，例如 `名字.转大写() → 名字.toUpperCase()`。必须只处理 `.中文方法名(`，不应粗暴替换所有同名文本。

### 字符串方法

| 中文方法 | 生成 JS 方法 | 说明 |
|---|---|---|
| 转大写 | toUpperCase | 转成大写 |
| 转小写 | toLowerCase | 转成小写 |
| 去空格 | trim | 去掉前后空格 |
| 包含 | includes | 判断是否包含 |
| 开始于 | startsWith | 判断是否以某段文本开头 |
| 结束于 | endsWith | 判断是否以某段文本结尾 |
| 切片 | slice | 截取一部分 |
| 分割 | split | 按分隔符切成数组 |
| 替换 | replace | 替换文本 |
| 重复 | repeat | 重复字符串 |

### 数组方法

| 中文方法 | 生成 JS 方法 | 说明 |
|---|---|---|
| 添加 | push | 在末尾添加元素 |
| 弹出 | pop | 移除并返回最后一个元素 |
| 开头添加 | unshift | 在开头添加元素 |
| 开头移除 | shift | 移除并返回第一个元素 |
| 包含 | includes | 判断是否包含元素 |
| 查找 | find | 找到第一个符合条件的元素 |
| 查找位置 | findIndex | 找到第一个符合条件元素的位置 |
| 筛选 | filter | 筛选元素 |
| 映射 | map | 把每个元素转换成新结果 |
| 遍历 | forEach | 对每个元素执行操作 |
| 排序 | sort | 排序 |
| 反转 | reverse | 反转 |
| 连接 | join | 连接成字符串 |
| 切片 | slice | 截取一部分 |
| 拼接 | splice | 增删改数组元素 |
| 归约 | reduce | 累积计算 |

---

## 9.6 示例：方法入口词

### `.zhjs`

```js
声明常量 名字 = " adrian ";
打印(名字.去空格().转大写());

声明变量 分数列表 = [80, 90];
分数列表.添加(100);
打印(分数列表);
```

### 生成后的 `.js`

```js
const 名字 = " adrian ";
console.log(名字.trim().toUpperCase());

let 分数列表 = [80, 90];
分数列表.push(100);
console.log(分数列表);
```

---

## 10. 转译规则

- 不替换字符串和注释中的中文入口词
- 长词优先，例如“否则如果”“声明变量”“声明常量”“输出错误”“是否不是数字”必须优先匹配
- `keyword / literal / operatorWord` 只在代码区域替换完整入口词
- `apiFunction` 只在“入口词(`”这种函数调用形式中替换
- `method` 只在“.入口词(`”这种点号方法调用形式中替换
- 中文变量名保留，不自动翻译变量名

### 原始 `.zhjs`

```js
打印("如果、返回、转大写 这些词在字符串里不应被替换");
```

### 生成后的 `.js`

```js
console.log("如果、返回、转大写 这些词在字符串里不应被替换");
```

---

## 11. Hover / 补全 / 文档要求

词表不只是转译用，它同时服务转译器、语法高亮、自动补全、Hover 中文解释、README 词表和后续文档。

因此每个入口词应该拥有结构化定义。

### keyword 示例定义

```ts
{
  zh: "声明变量",
  target: "let",
  kind: "keyword",
  category: "declaration",
  level: "foundation",
  description: "声明一个可以重新赋值的变量。",
  exampleZh: "声明变量 分数 = 80;",
  exampleJs: "let 分数 = 80;",
  completion: "声明变量 ${1:变量名} = ${2:值};"
}
```

### method 示例定义

```ts
{
  zh: "转大写",
  target: "toUpperCase",
  kind: "method",
  category: "string",
  level: "foundation",
  description: "把字符串转成大写。",
  exampleZh: "名字.转大写()",
  exampleJs: "名字.toUpperCase()"
}
```

---

## 12. 基础中文错误解释

| 错误类型 | 中文解释 |
|---|---|
| ReferenceError | 变量没有定义。可能原因：你使用了一个还没有声明的变量，或者变量名写错了。 |
| SyntaxError | 语法错误。可能原因：括号、大括号、引号、分号、逗号或结构不完整。 |
| TypeError | 类型错误。可能原因：你把一个不是函数的值当成函数调用，或者访问了不存在的属性。 |

必须保留原始英文错误，不要隐藏真实错误。

---

## 13. README 第一屏要求

README 第一屏必须包含：

- 项目名
- 一句话定位
- 中文入口词代码
- 生成后的标准 JavaScript
- 明确说明不是中文编程语言
- 明确说明 `.zhjs` 不直接运行，真正运行 `.js`
- 明确说明符号和语法不变

建议标语：

```text
Use Chinese entry words to learn real JavaScript.
用中文入口词学习真实 JavaScript。
```

---

## 14. V0.1 验收标准

V0.1 必须满足：

- VS Code 能识别 `.zhjs`
- `.zhjs` 有基础高亮
- 中文入口词有自动补全
- Hover 能显示中文解释
- 能从 `.zhjs` 生成同名 `.js`
- 生成 `.js` 顶部有说明注释
- 能一键运行生成后的 `.js`
- Output 面板能显示运行结果
- 能查看生成后的标准 JavaScript
- 字符串和注释中的中文入口词不被替换
- 支持 `keyword / literal / operatorWord / apiFunction / method` 五类转译
- 覆盖 JavaScript 基础入口词表
- README 能让用户 3 秒内理解项目

---

## 15. 最终一句话

> **ZhCode Bridge V0.1 不做完整 JavaScript 生态，但不做阉割版。它必须完整覆盖 JavaScript 基础入口词，通过 `.zhjs` 中文表层文件生成标准 `.js`，让中文初学者获得完整的基础编程体验，并最终迁移到真实 JavaScript 生态。**
